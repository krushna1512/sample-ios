opt_out_usage
platform :ios do
  lane :build do
    gym(
      scheme: "SampleApp",
      export_method: "development",
      export_options: {
        signingStyle: "automatic"  # or "none" if you want to skip signing
      },
      clean: true,
      skip_profile_detection: false    
    )
  end
end
