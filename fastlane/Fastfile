opt_out_usage
platform :ios do
  lane :build do
    gym(
      scheme: "SampleApp",  # Replace with your scheme name
      clean: true,
      export_method: "development",
      export_options: {
        signingStyle: "none"
      }
    )
  end
end
